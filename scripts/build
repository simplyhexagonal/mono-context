#!/usr/bin/env bash

rm -rf dist/*
esbuild src/index.ts --bundle --target=chrome60,firefox60,safari12,edge18 \
  --outfile=dist/multi-replace.js --sourcemap --format=iife --global-name=MonoContext \
  && esbuild src/index.ts --bundle --target=chrome60,firefox60,safari12,edge18 \
  --outfile=dist/multi-replace.min.js --minify --format=iife --global-name=MonoContext \
  && tsc -d --emitDeclarationOnly --rootDir src --outDir dist \
  && mv dist/index.d.ts dist/multi-replace.d.ts \
  && refup dist/multi-replace.js MonoContext \
  && refup dist/multi-replace.min.js MonoContext \
  || exit 1
